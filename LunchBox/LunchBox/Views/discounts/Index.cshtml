@model IEnumerable<LunchBox.discount>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.percentage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.couponCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.percentage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.couponCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.disId }) |
            @Html.ActionLink("Details", "Details", new { id=item.disId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.disId })
        </td>
    </tr>
}

</table>
